name: Email Step Test

on:
  push:
    branches: [ main ]  # Or whichever branch you want

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: 24f1002255@ds.study.iitm.ac.in
        run: echo "Hello, world!"
